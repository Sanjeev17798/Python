1. Find the largest file in a directory

Scan a folder and print the file with the maximum size.

2. Find duplicate files

Compare files using hash (MD5/SHA256) and detect duplicates.

3. Rename multiple files automatically

Example: rename all .txt files to file_1.txt, file_2.txt etc.

4. Delete files older than X days

Useful for log cleanup automation.

5. Monitor a directory for new files

Continuously check a folder and print when a new file appears.

6. Count lines of code in all .py files in a project

Traverse directories and calculate total code lines.

7. Read a log file and extract ERROR lines

Filter logs and print only lines containing "ERROR".

8. Create a simple disk usage report

Print directory name + total size (similar to du command).

9. Find all empty files in a directory

Check files with 0 bytes and list them.

10. Check if a website is reachable

Simple HTTP status check script (useful for monitoring).

11. Get system information

Print:

CPU usage

Memory usage

Disk usage

(using psutil).

12. Backup a directory

Copy all files from one folder to another with timestamp.